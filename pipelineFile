pipeline {
    agent any

    stages {
        stage('scm') {
	    agent { label "Controller" }
            steps {
		dir ("appname") {
			git credentialsId: 'balaji1975', branch: "main",
			url: "git@github.com:balaji1975/devops.git";
		}
            }
        }
    }
}
